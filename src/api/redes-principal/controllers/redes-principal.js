'use strict';

/**
 * redes-principal controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::redes-principal.redes-principal');
